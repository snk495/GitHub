export enum HeaderSelectors {
    lumaLogo = '.logo',
    searchInput = '#search',
    searchButton = 'button[title="Search"]',
    pageTitle = '.page-title',
    signInLink = 'a:has-text(" Sign In ") >> nth=0',
    createAcountLink = 'a:has-text("Create an Account") >> nth=0',
    cartButton = '.showcart',
    cartDropdownDialog = '.block-minicart',
    globalMessage = '.message.global',
}